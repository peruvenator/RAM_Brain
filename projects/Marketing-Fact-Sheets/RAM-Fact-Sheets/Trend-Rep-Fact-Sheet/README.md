# Trend Rep Fact Sheet

HTML fact sheet for the RAM trend replication strategy.

See [CLAUDE.md](CLAUDE.md) for workflow and folder layout.

## Quick Start

1. Source data lives in `data/` (Excel time series) + `C:\Users\RodrigoGordillo\S3_Data\` (additional series)
2. Reference layouts in `references/` (existing RAM fact sheet PDFs)
3. Build scripts go in `build/`
4. Final HTML lands in `output/`
