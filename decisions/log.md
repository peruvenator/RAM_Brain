# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-08-26] DECISION: Rebuild the research blog landing page (Blog_Indexing_LP) as a clone of the Product Articles widget (design 6a) with the brand blue accent and blue contour pattern only, generated from blog-index.json by build_research_widget.py | REASONING: One visual system across the Product Articles, Literature, and Research pages; blue vs teal distinguishes research from product content; a build step keeps 71+ posts data-driven instead of hand-rotated markup | CONTEXT: Rodrigo asked to replicate the product blog design for the research widget keeping black/blue instead of black/green. Deliverable is research-articles-embed.html; old blog-landing-page_divi.html stays live until handoff
[2026-08-26] DECISION: Research Articles widget keeps the live page's structure (search + chips, Latest Posts and Start Here as three navy backdrop cards each with one larger, category tiles by topic / by strategy) styled with the Literature widget's tile and chip elements; teal stays the accent, the blue contour PNG is the only backdrop | REASONING: Rodrigo rejected the first pass (product-widget hero + card archive, blue accent): the categorization must stay as it is today and 'black and blue' referred to the backdrop pattern only | CONTEXT: Supersedes the earlier 2026-08-26 entry for this widget. Deliverable research-articles-embed.html regenerated by build_research_widget.py

[2026-09-08] DECISION: Repoint update_margin_utilization.py WEIGHTS_ROOT from backtest-sims-dev to backtest-sims, add staleness checks that exit non-zero (live margins or weights older than 5 days, or zero rows appended), and fully regenerate all 12 strategy margin CSVs | REASONING: Weights moved folders on 2026-07-03 and the incremental job silently reported 0 new rows for two months with exit 0, so no alert fired; margin CSVs were frozen at 2026-07-02 | CONTEXT: Found while pulling margin methodology for the Gold & Managed Futures margin widget. Backups in Margin_utilization_backup_20260908. Dropbox margin feed separately stopped 2026-08-19 (upstream, data team). Pre-2020 static rates left unchanged in the shared pipeline; a volatility-scaled backfill was prototyped project-locally only

[2026-09-14] DECISION: Product Articles widget Latest section becomes a single row (hero 50% + two mini cards 25% each) showing three articles instead of five; everything older lives only in All Articles | REASONING: Rodrigo wants the newest article to dominate and the blog roll below to carry the archive; a 4-up mini row under a full-width hero pushed All Articles too far down as the archive grows | CONTEXT: Added with the two Sep 10 glidepath posts (hosted on returnstacked.com, post-password protected; summaries provisional, tickers pending). V3 handoff on hold until copy is approved.

[2026-09-14] DECISION: Product Articles widget Latest section revised the same day to two articles: hero two-thirds width (with its strategy tag pill) + second article one-third width including its summary; supersedes the 50/25/25 three-article layout | REASONING: Rodrigo wanted the strategy tag visible on the main article and the second article to carry its description rather than a title-only mini | CONTEXT: Same glidepath articles; provisional copy and tickers still pending.

[2026-09-14] DECISION: Product Articles widget Latest section settles on three articles: hero 50% (strategy tag pill, 26px title) + two white cards 25% each that carry their summaries; supersedes both earlier layouts from today | REASONING: Rodrigo wanted the third article back but liked the summary-bearing card | CONTEXT: Glidepath articles still provisional (copy/tickers pending article text).

[2026-09-14] DECISION: Research Articles widget search rebuilt as a context-driven engine (Porter stemmer, two-way prefix, one-edit typo tolerance, build-time synonym groups in search-synonyms.json, any-word fallback with a note) while keeping the filter-in-place layout. | REASONING: Sales team reported taxation 0 hits, taxes 1, tax 6; the old stemmer only stripped a trailing s. Query-side ranking was rejected as a larger layout change. | CONTEXT: projects/return-stacked-website/Blog_Indexing_LP; embed grows to 263 KB raw / 37 KB gzip; test_search.js guards the behavior.

[2026-09-14] DECISION: Blog catalog keyword and key-sentence extraction ignores chart-source lines, performance disclaimers, index definitions, and the audio-player caption; blog-index.json refreshed for the 62 scraped posts via refresh_keywords.py. | REASONING: "Why Bonds Still Belong" surfaced for "taxes" solely from "Index returns are gross of all fees, taxes, and transaction costs" under a chart; Rodrigo ruled that search context is title, summary, and body only. | CONTEXT: Blog_Indexing_LP; taxes now returns 4 articles (was 6); test_search.js asserts the two disclaimer-only posts stay out.

[2026-09-15] DECISION: The Research Articles search engine became a shared skill, rs-index-search (.claude/skills/rs-index-search/): one canonical search-core.js and one search-synonyms.json, spliced into each widget between RS-SEARCH-CORE markers by sync_search_core.py, with a config-driven regression runner (test_search.js + a search-tests.json per widget). Ported to the Product Articles (rsArt) and Literature (rsLit) widgets the same day; the Research Articles build now refuses to run on a stale core. | REASONING: Rodrigo asked whether to drop an MD file into the other projects or build a skill; the search work is code (Porter stemmer, typo tolerance, synonym groups, any-word fallback), so a description alone would drift across three hand-edited copies, and a skill also covers future index sections on the website. Synonyms run at runtime inside the core so the hand-maintained widgets need no build step. | CONTEXT: Blog embed 285 KB to 294 KB; Product embed 35 KB to 47 KB; all three search-tests.json ALL PASS. ETF tickers are deliberately not synonym triggers (Literature compliance: a ticker must never surface Manager Research rows).
[2026-09-16] DECISION: Literature widget search keywords are curated from each PDF's full text (25-45 terms per row) rather than shipping excerpts; Return Stacked Primer restored as the first Presentations row only. | REASONING: Curated terms give most of the recall with no engine change, no layout noise from false positives, and a 4 KB file delta vs 15-20 KB for excerpts; Primer is still the best first deck for advisors but should not compete with the Case for NOT Making Room hero. | CONTEXT: Source list is the Notion Publications List rows tagged Literature Page; regression queries per doc added to Literature_PDF_Index/search-tests.json. Stopword handling flagged as a follow-up for the shared rs-index-search core.

[2026-09-19] DECISION: Created project projects/Data managment/Hubspot_company_fuzzy_match | REASONING: Enrich 16k+ HubSpot company names with FINTRX data for Return Stacked sales enablement; fuzzy match with rapidfuzz, review band before any CRM write | CONTEXT: Source Excel and FINTRX match universe still to be loaded

[2026-09-19] DECISION: New RAM Brain credentials live in the 1Password vault "ReSolve" and are read at runtime via the 1Password CLI (service account token stored as a Windows user env var, not in any file). Legacy root .env is left as-is, not ported. | REASONING: Plaintext .env sat inside Dropbox sync; op run injects secrets per process so keys never touch the repo, chat, or transcripts, and the service account covers scheduled tasks without Windows Hello prompts. | CONTEXT: SOP at references/sops/1password-credentials.md covers naming convention (op://ReSolve/<Item>/credential) and the op.env template pattern.

[2026-09-19] DECISION: Forked Adam's FINTRX Python SDK into references/fintrx/ as a shared Windows package (1Password auth, no Keychain), bare client only; matching, holdings taxonomy and bulk runners stay in consuming projects. | REASONING: One-off enrichment jobs need bulk pagination and 13-F endpoints the FINTRX MCP connector does not expose; a shared package stops each project re-porting the client, and keeping it bare avoids building toolkit code before the use cases are fleshed out. | CONTEXT: Session token is captured from a dedicated debug Chrome profile (Chrome 136+ blocks debugging on normal profiles) and stored on 1Password item Fintrix; required a new read+write service account because the original was read-only. 50/50 tests pass on Rodrigo's seat. Adam's pipeline in rs-sales-synthesis is untouched. First consumer: projects/Data managment/Hubspot_company_fuzzy_match.
